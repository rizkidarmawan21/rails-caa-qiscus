<%= form_with(model: caa_webhook_log) do |form| %>
  <% if caa_webhook_log.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(caa_webhook_log.errors.count, "error") %> prohibited this caa_webhook_log from being saved:</h2>

      <ul>
        <% caa_webhook_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
